export interface DeliveryOption {
  id: string;
  title: string;
  description: string;
  price: number;
}

export interface SampleProduct {
  id: string;
  name: string;
  category: string;
  image: string;
}